package gameObjects;

import javafx.scene.canvas.GraphicsContext;
import standard.FlappyBird;

public class BirdExt extends Bird{
	int remoteIndex;
	
	public BirdExt(FlappyBird flappyBird,double screenWidth, double screenHeight, GraphicsContext ctx, int remoteIndex) {
		super(flappyBird,screenWidth, screenHeight, ctx);
		this.remoteIndex=remoteIndex;
		// TODO Auto-generated constructor stub
	}
	
	@Override
	public void update(long now) {

		double[] tmp =  flappyBird.getPosExt(remoteIndex);
		this.sprite.setPos(tmp[0], tmp[1]);
		
		if (flappyBird.isGameStarted() && tmp[1]!=448) { //Wenn der Vogel am Boden ist soll er nicht mehr animiert werden; Test ob Vogel tot wäre zu aufwendig
            if (now - prevTime > 90000000) {
                updateSpriteAnimation();
                prevTime = now;
            }
		}
	}
	
	public void render() {
		sprite.render();
	}

}
